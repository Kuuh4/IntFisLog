/* autogenerated by Processing revision 1286 on 2022-11-11 */
import processing.core.*;
import processing.data.*;
import processing.event.*;
import processing.opengl.*;

import java.util.HashMap;
import java.util.ArrayList;
import java.io.File;
import java.io.BufferedReader;
import java.io.PrintWriter;
import java.io.InputStream;
import java.io.OutputStream;
import java.io.IOException;

public class bubble extends PApplet {

Bubble[] bubbles = new Bubble[4];

 public void setup(){
    /* size commented out by preprocessor */;
  //b0 = new Bubble(x,y,diameter,colorr);  
    bubbles[0] = new Bubble(-150,50,64,color(0xFFFFFFFF));
    bubbles[1] = new Bubble(-50,50,64,color(0xFFFFFFFF));
    bubbles[2] = new Bubble(50,50,64,color(0xFFFFFFFF));
    bubbles[3] = new Bubble(150,50,64,color(0xFFFFFFFF));

    noStroke();

}

 public void draw(){
    background(128);
    
    bubbles[0].mover();
    bubbles[0].ascend();
    bubbles[0].debug();
    
    bubbles[1].mover();
    bubbles[1].ascend();
    bubbles[1].debug();
    
    bubbles[2].mover();
    bubbles[2].ascend();
    bubbles[2].debug();
    
    bubbles[3].mover();
    bubbles[3].ascend();
    bubbles[3].debug();

}

 public void mousePressed(){
    bubbles[0].pop();
    bubbles[1].pop();
    bubbles[2].pop();
    bubbles[3].pop();
}
class Bubble{
    float x;
    float y;
    float diameter;
    int colour;
    
    Bubble(float tX, float tY,float tdiameter, int tcolour){
        x=tX+width/2;
        y=tY+height;
        diameter = tdiameter ;
        colour = tcolour;

        ellipseMode(CENTER);
    }

     public void ascend(){
        if (y>diameter/2) {
            y--;
        }
        x=x+random(-2,2);
    }

     public void mover(){
        fill(colour);
        ellipse(x,y,diameter,diameter);
        //line(0 , y, width, y);
    }

     public void pop(){
        colour = color(random(0,255),random(0,255),random(0,255));
    }

     public void debug(){
        println(colour);
    };
}


  public void settings() { size(400, 400); }

  static public void main(String[] passedArgs) {
    String[] appletArgs = new String[] { "bubble" };
    if (passedArgs != null) {
      PApplet.main(concat(appletArgs, passedArgs));
    } else {
      PApplet.main(appletArgs);
    }
  }
}
